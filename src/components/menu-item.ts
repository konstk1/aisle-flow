// Shared styling for rows inside the app's popover menus (app-nav,
// store-picker, sign-out). Text color is left to the call site, which varies
// it by active/current state.
export const menuItemClass =
  "flex min-h-11 items-center gap-3 rounded-xl px-3 text-sm font-semibold transition hover:bg-ink-50";

export const menuItemIconClass = "size-4 text-ink-350";
